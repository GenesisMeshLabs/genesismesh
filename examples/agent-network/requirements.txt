# Optional dependencies for examples/agent-network/llm_agent.py
#
# Genesis Mesh itself has no LLM dependency. This file is installed only by
# operators running the LLM-backed responder example:
#
#   pip install -r examples/agent-network/requirements.txt
#
# LiteLLM provides a single async API surface across OpenAI, Azure OpenAI,
# Anthropic, Bedrock, Vertex, Ollama, Together, Groq, Mistral, vLLM, and
# many others. Adding a new provider means changing the LLM_MODEL env var,
# not editing code.
litellm>=1.55

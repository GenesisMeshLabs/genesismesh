[pytest]
testpaths = genesis_mesh/tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -v --tb=short
asyncio_mode = auto
markers =
    integration: end-to-end multi-runtime tests (slower, isolated from unit tests)
